namespace: paperless
api:
  spec: ./api/paperless.yaml
  basePath: $BASE_URL
  auth:
    type: http_bearer
    requireKeys:
      - id: token
        displayName: Token
        type: string
title: paperless
