category: Text-to-Speech
title: "TTS: Text to Speech"
description: >-
  Generates audio from the input text. The maximum length of the text to generate audio for is 4096 characters.
apiNamespace: openai
apiOperationId: createSpeech
displayNamespace: openai
displayOperationId: createSpeech
inputs:
  model:
    default: tts-1
    choices:
      - tts-1
      - tts-1-hd
